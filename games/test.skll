&px = 0; 
&py = 0; 
&enemies = [[1,1], [2,2], [3,3]];
&friendlies = [[5,1], [5,2], [5,3]];

print(&enemies);

proc &update () {
};

proc &key_left () {
  &px = (&px - 1); 
};

proc &key_right () {
  &px = (&px + 1); 
};

proc &key_up () {
  &py = (&py + 1); 
};

proc &key_down () {
  &py = (&py - 1); 
};

proc &key_space () {
};
